# lazors-group-project

## Example Files

All .bff test files (e.g., `dark_1.bff`, etc.) are stored in the `examples` branch of this repository.  
To access them, switch to the `examples` branch on GitHub or clone it separately.
